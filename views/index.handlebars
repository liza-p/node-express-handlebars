{{!-- Setup the `index.handlebars` to have the template that Handlebars can render onto.
Create a button in `index.handlebars` that will submit the user input into the database. --}}
<div class = "container" style = "width: 800px" >
     <div class="card">
      <img src="/assets/img/travel-small.jpg" class="card-img-top" alt="travel">
      <div class="card-body">
        <h5 class="card-title text-center main-title">Travel Bucket List</h5>
        
            <form class="form-inline addDestination">
            
            <div class="form-group mx-sm-3 mb-2">
                <input  type="text" class="form-control" id="input" placeholder="Enter Destination">
            </div>
            <button type="submit" class="btn btn-warning mb-2">Add</button>
            </form>
            <div class = "row">
                <div class = "col-12 col-sm-6"> 
                     {{>wannaGoCard}}
                </div>
                 <div class = "col-12 col-sm-6"> 
                     {{>visited}}
                </div>

            </div>
           
      </div>
    </div>
   

   
</div>
